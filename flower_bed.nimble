# Package

version       = "0.1.0"
author        = "Locria Cyber"
description   = "A graphical algorithm editor"
license       = "nobs"
srcDir        = "src"
bin           = @["flower_bed"]
binDir        = ".build"

# Dependencies

requires "nim >= 1.6.2"
requires "nimraylib_now >= 0.14.2"