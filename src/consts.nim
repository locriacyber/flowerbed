import nimraylib_now

const screenWidth* = 800
const screenHeight* = 450
const playground*: Rectangle = (x: 0.0, y: 0.0, width: screenWidth.float*0.75, height: screenheight.float)
const cursor_circle_size* = 10.0
const default_node_size* = (x: 40.0, y: 60.0)